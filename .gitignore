# root-level only: a bare `make-icons.py` hid same-named scripts in any subfolder
/make-icons.py
__pycache__/
*.pyc
.DS_Store

# Secrets / credentials -- security hardening 2026-06-19 (never commit these)
.env
.env.local
.env.*
*.pem
*.key
auth.json
.credentials.json
service-account*.json
*-token.txt
.agent/
